--index-url https://download.pytorch.org/whl/cu118
--extra-index-url https://pypi.org/simple

# Core PyTorch stack (CUDA 11.8)
torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2
xformers==0.0.23.post1

# Hugging Face / diffusion toolchain
huggingface-hub==0.25.2      # pinned for cached_download compatibility
transformers==4.30.2
diffusers==0.25.1
accelerate==0.24.1
safetensors==0.4.2

# Arrays / math
numpy==1.26.4
scipy==1.11.4

# Gradio pair pinned to avoid the json_schema 'bool is not iterable' crash
# (Upgrade both together if you ever change these.)
gradio==4.44.1
gradio_client==1.3.0
pydantic==2.10.6

# Vision / plotting / IO
# Matplotlib 3.7.x supports Python 3.8; 3.8.x for Python 3.9+.
matplotlib==3.8.4
imageio==2.33.1
decord==0.6.0
einops==0.7.0
kornia==0.7.1
h5py==3.10.0

# Config / utils / UI
omegaconf==2.3.0
tqdm==4.66.1
wandb==0.16.6
soundfile==0.12.1
moviepy==1.0.3
